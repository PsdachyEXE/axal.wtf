# ── Spring Boot backend ───────────────────────────────────────────────────────
# Production: set this to the deployed Spring Boot backend origin.
# Development: leave empty — Vite's dev proxy (see vite.config.js) forwards
#              all /api/* requests to http://localhost:8080 automatically.
VITE_API_URL=
